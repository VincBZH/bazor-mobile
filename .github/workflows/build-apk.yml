name: Build BAZOR APK

on:
  push:
    branches: [ main, 'feature/**' ]
    paths:
      - 'android/**'
      - '.github/workflows/build-apk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Gradle 8.10.2
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build APKs
        run: gradle :app:assembleDebug :watchapp:assembleDebug --stacktrace

      - name: Rename APKs
        run: |
          cp app/build/outputs/apk/debug/app-debug.apk ../BAZOR-Tool.apk
          cp watchapp/build/outputs/apk/debug/watchapp-debug.apk ../BAZOR-Watch.apk

      - name: Upload BAZOR Tool
        uses: actions/upload-artifact@v4
        with:
          name: BAZOR-Tool-APK
          path: BAZOR-Tool.apk
          if-no-files-found: error

      - name: Upload BAZOR Watch
        uses: actions/upload-artifact@v4
        with:
          name: BAZOR-Watch-APK
          path: BAZOR-Watch.apk
          if-no-files-found: error
